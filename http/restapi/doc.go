// Package restapi provides a chi-based HTTP server with built-in middleware
// (structured request logging, Prometheus metrics, recoverer), a /ping
// health endpoint, and optional /debug pprof support.
package restapi
